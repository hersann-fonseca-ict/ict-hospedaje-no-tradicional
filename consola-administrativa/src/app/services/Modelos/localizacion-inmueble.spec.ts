import { LocalizacionInmueble } from './localizacion-inmueble';

describe('LocalizacionInmueble', () => {
  it('should create an instance', () => {
    expect(new LocalizacionInmueble()).toBeTruthy();
  });
});
